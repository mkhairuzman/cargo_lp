node_modules/
dist/
build/
.cache/
.env
.env.*
!.env.example
.vite/
.DS_Store
Thumbs.db
vite.config.*.timestamp-*
*.tsbuildinfo
*-debug.log*
logs/
*.log
.claude-replay.*
.tmp-*
/.figma/design-context/

# Platform-managed agentplat state, not project source. In the
# client-initiated single-repo workspace layout (make's `Path: "."` spec) the
# repo root IS the workspace root, so these land inside this repo rather than
# beside it.
/.plugins/
/.opencode-skills/

# Local design/audit artifacts; never publish.
/.figma/
/.chrome-audit-temp/
/audit-screenshots/
/*AUDIT*.md
/*FRESH_EXPORT_CHECK*.md
/*.zip

# Local agent instructions.
/AGENTS.md
/CLAUDE.md
/.mise.toml
/.gitattributes
/src/imports/pasted_text/

# Local Python scratch.
/__pycache__/
*.py[cod]
